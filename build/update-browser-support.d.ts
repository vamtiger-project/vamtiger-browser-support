import { IUpdateBrowserSupport } from './types';
export default function updateBrowserSupport({ supportFile, supported, polyfill, polyfills, polyfillChain, error, wait }: IUpdateBrowserSupport): Promise<void>;
